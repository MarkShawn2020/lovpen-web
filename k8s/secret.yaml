apiVersion: v1
kind: Secret
metadata:
  name: lovpen-web-secrets
  namespace: lovpen-web
type: Opaque
data:
  # Base64 encoded values - replace with actual values
  DB_PASSWORD: '' # echo -n "your-password" | base64
  CLERK_SECRET_KEY: '' # echo -n "your-clerk-secret" | base64
  NEXTAUTH_SECRET: '' # echo -n "your-nextauth-secret" | base64
  SENTRY_DSN: '' # echo -n "your-sentry-dsn" | base64
